# P51 收尾报告（53.txt + 组元卡统一）

## A. 修改文件
`calcs/gating.js`（systemType 文案收紧）；`js/views/gatingView.js`（比例展示/ⓘ 精简/防御/组元结构）；
`css/app.css`（组元卡卡片样式 + .judge.warn 背景补全——上轮遗留）

## B. 工程逻辑
1. **优化提交通用防御（53 §一）**：拒绝条件由"Fg≤Fs 且 Fg≤Fr"扩展为**优化前后 chokePosition 任一变化** →
   拒绝提交、保留优化前几何、提示"优化后阻流位置发生变化，已取消本次优化，请重新调整浇注比例/上游截面积。原设计结果保持不变。"
   （未修改优化算法；为未来防御机制。phase53-T15 验证 ingate→sprue 变化被拒条件成立、原几何重算不变、sprue 稳定场景可接受）
2. **systemType 文案收紧（53 §二）**：ingate→"封闭式/加压式特征：内浇口为阻流截面"；sprue→"开放式/非加压式特征：直浇道为阻流截面"；
   runner→"横浇道阻流：上游截面控制流量"；boundary→"阻流位置临界：建议人工复核"——面积判断不再包装成绝对物理分类。

## C. 比例预设信息展示（53 §三-八）
- **下拉选项**：每个预设显示"名称（S直:S横:S内 = 1:2:0.85）"，比例从 RATIO_PRESETS 数据实时生成（未手写死）；
  option 显式 value=预设名（计算解析不受文本影响）
- **选中说明行**：下拉下方常显"名称 · S直:S横:S内 = x:y:z · 适用：…"（适用文本依据现有自动选择条件整理，引用代码既有 >200kg 阈值，未新造）
- **ⓘ 弹窗精简**：简介句（截面积关系影响阻流/流速/平稳性）→"系统会自动推荐"说明（非强制）→ 预设一览
  （每项：名称+比例+适用+阻流注释）→ 收尾一句"不知道怎么选时，直接采用系统自动推荐的预设即可"。无 Campbell 理论展开
- **报告**：基本参数"比例预设"单元格显示"预设名 · S直:S横:S内 = x:y:z"（UI 显示=计算使用=报告记录，三处一致，T13 验证）
- 自动选择逻辑/阈值/比例本身**零修改**（T14 锁定：灰铁 50→封闭常用、灰铁 300→封闭大件、球铁 50→开放标准、
  球铁 300→开放宽大、铝→铝合金属、铜→开放标准）

## D. 组元卡统一（口头补充，仅显示）
组元尺寸卡（内浇道/横浇道/直浇道/排气）输出行加 `.gcards` 容器 → 卡片样式与结果卡统一：
浅底 #f8fafc + 1px 边框 + 10px 圆角 + 6px 行距（ok/warn/bad 状态保留各自淡色背景）；
CSS 追加 app.css 尾部，逻辑零改动（set() 更新机制不变，10 行已卡化）。

## E. 测试
- 新增 phase53 5 项：T11 预设数据完整性（6 预设 3 正数比例）/T14 自动选择逻辑不变（6 断言锁定）/
  T15 choke 变化防御（ingate→sprue 拒绝条件 + 原几何保留 + sprue 稳定可接受）/T16 systemType 文案 4 条精确断言/
  T17a UI 预设名与 calc 解析比例一致（全预设遍历）
- 浏览器（T12/T13/组元样式/ⓘ）：预设 option 含比例、选中说明行（名称+比例+适用）、报告比例一致、
  .gcards 10 行卡样式实测（radius 10px、浅底）、systemType 文案、ⓘ 精简内容（含"预设一览"与收尾句、无 Campbell）——全部通过
- **全量回归：122 / 0 passed**

## F. 不确定项与如实说明
1. **默认输入（真实件 46.8kg×2/Ho180）实际处于 boundary**：Fs_act=452.4 vs Fg=450（差 0.53% <1% 容差）——
   理论封闭式有意 15% 差被直浇道直径取整（round 24mm）压缩到 0.5% → 系统如实显示"阻流位置临界：建议人工复核"
   且不提供优化。判定正确反映实际几何并列，但默认场景第一屏即提示复核属 P51 判定规则的直接后果（未改规则；
   若需默认场景顺畅，未来可评估判定容差口径——不在本轮范围）。
2. ⓘ 预设"适用"文本为按代码条件整理的一句话说明（非 preset 自带 description 字段——当前数据结构无该字段，
   53 §五 允许按现有名称/条件整理）；所有数值（比例/阈值）均取自代码常量。
